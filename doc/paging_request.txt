○Pagingサポートの実装

優先度 高(かならず実装する)
・MMUを有効にする。
・キャッシュを使えるようにする(テキスト/データ)
・メモリ属性(デバイス,ノーマル等)を正しく設定する。
・ノーマルメモリはすべてcacheableにする？(VideoFrmaeBufferなどはどうする？)
・ノーマルメモリは最初にすべてマッピング
・テキスト/ReadOnly領域は書き換え不可にする。

優先度 低(気が向いたらそのうち)
・キャッシュ制御APIを実装(invalidate/clean?もっと抽象的なもの？)
・Linuxでのioremap()のようなAPIを用意する。(デバイス対応のみで十分?)
・BSSクリア時にキャッシュ有効化したい。(後回しかな？)

当面(あるいは将来も)やらない
・アドレス変換は(とりあえず)実装しない。(論理アドレス==物理アドレス)
・L2(PL-310)はサポートしない。(将来的にはなくなるだろう)
